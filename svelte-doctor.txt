
  svelte-doctor v0.1.9



  ✗ Legacy lifecycle import detected in a Svelte 5 project (3)
    Replace `onMount`, `onDestroy`, `beforeUpdate`, and `afterUpdate` with `$effect()`
    src/routes/invite/[id]/+page.svelte:2
    src/routes/org/[slug]/+layout.svelte:2
    src/routes/org/+page.svelte:2

  ⚠ Repeated allocation inside `$derived()` can churn memory and recomputation cost (50)
    Avoid allocating new arrays or objects in heavy `$derived()` blocks unless the allocation is required and bounded.
    src/lib/components/action-command.svelte:43
    src/lib/components/floating-toolbar.svelte:23
    src/lib/components/issue-search-command.svelte:64,65,69,70,71,72,73
    src/lib/components/issues/issue-list-body.svelte:66
    src/lib/components/issues/issue-row.svelte:28,30
    src/lib/components/issues/priority-indicator.svelte:31
    src/lib/components/issues/status-badge.svelte:33
    src/lib/components/issues/template-field-renderer.svelte:40,41,91
    src/lib/components/ui/button-group/button-group-text.svelte:15
    src/lib/components/ui/chart/chart-container.svelte:20
    src/lib/components/ui/chart/chart-style.svelte:6
    src/lib/components/ui/chart/chart-tooltip.svelte:71
    src/lib/components/ui/field/field-error.svelte:32,33
    src/lib/components/ui/field/field-separator.svelte:16
    src/lib/components/ui/item/item.svelte:46
    src/lib/components/ui/sidebar/sidebar-group-action.svelte:16
    src/lib/components/ui/sidebar/sidebar-group-label.svelte:16
    src/lib/components/ui/sidebar/sidebar-menu-action.svelte:18
    src/lib/components/ui/sidebar/sidebar-menu-button.svelte:59
    src/lib/components/ui/sidebar/sidebar-menu-sub-button.svelte:20
    src/lib/components/workspace-sidebar.svelte:65,66
    src/routes/org/[slug]/+page.svelte:13
    src/routes/org/[slug]/issues/[number]/+page.svelte:35,65,66
    src/routes/org/[slug]/issues/new/+page.svelte:27,28,88
    src/routes/org/[slug]/settings/+page.svelte:74,215,216,217,218,221
    src/routes/org/[slug]/settings/templates/new/+page.svelte:51
    src/routes/sign-in/+page.svelte:21,23
    src/routes/sign-up/+page.svelte:22,24

  ⚠ Block nesting depth is 4 (max: 3). (11)
    Extract nested sections into separate components to improve readability.
    src/lib/components/issues/template-field-renderer.svelte:267,277
    src/lib/components/ui/calendar/calendar.svelte:97
    src/lib/components/ui/field/field-error.svelte:51
    src/lib/components/ui/range-calendar/range-calendar.svelte:94
    src/routes/org/[slug]/issues/[number]/+page.svelte:236
    src/routes/org/[slug]/settings/+page.svelte:710,758,890,938,951

  ⚠ Block nesting depth is 5 (max: 3). (2)
    Extract nested sections into separate components to improve readability.
    src/lib/components/issues/template-field-renderer.svelte:282,293

  ⚠ Avoid `fetch()` in component scripts — use SvelteKit `load` functions or form actions instead. (2)
    Move data fetching to `+page.ts` / `+page.server.ts` load functions, or use form actions for mutations.
    src/lib/components/issues/template-field-renderer.svelte:114
    src/routes/org/[slug]/settings/+page.svelte:280

  ⚠ Inline object or array literal in template expression causes re-creation on every render (12)
    Extract the value into a `$derived` or a module-level constant to avoid allocating a new reference each render cycle.
    src/lib/components/ui/alert-dialog/alert-dialog-media.svelte:16
    src/lib/components/ui/input-group/input-group-text.svelte:15
    src/lib/components/ui/input-otp/input-otp-separator.svelte:19
    src/lib/components/ui/menubar/menubar-item.svelte:22
    src/lib/components/ui/navigation-menu/navigation-menu-link.svelte:15
    src/lib/components/ui/pagination/pagination-ellipsis.svelte:17
    src/lib/components/ui/pagination/pagination-next-button.svelte:23
    src/lib/components/ui/pagination/pagination-prev-button.svelte:23
    src/lib/components/ui/select/select-scroll-down-button.svelte:16
    src/lib/components/ui/select/select-scroll-up-button.svelte:16
    src/lib/components/ui/table/table-cell.svelte:13
    src/lib/components/ui/table/table-head.svelte:13

  ⚠ {#each} block is missing a key expression (3)
    Add a key expression like `{#each items as item (item.id)}` so Svelte can efficiently diff list updates instead of re-creating DOM nodes.
    src/lib/components/workspace-sidebar.svelte:150,161
    src/routes/org/[slug]/settings/+page.svelte:590

  ⚠ Component has 334 meaningful lines (limit: 300). Consider breaking it into smaller components.
    Large components are harder to maintain and test. Extract logical sections into child components or shared utilities.
    src/lib/components/workspace-sidebar.svelte:1

  ⚠ Component has 326 meaningful lines (limit: 300). Consider breaking it into smaller components.
    Large components are harder to maintain and test. Extract logical sections into child components or shared utilities.
    src/routes/org/[slug]/issues/[number]/+page.svelte:1

  ⚠ `title` is wrapped in `$state` but never mutated or reassigned
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/org/[slug]/issues/new/+page.svelte:40

  ⚠ `description` is wrapped in `$state` but never mutated or reassigned (2)
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/org/[slug]/issues/new/+page.svelte:41
    src/routes/org/[slug]/settings/templates/new/+page.svelte:34

  ⚠ `priority` is wrapped in `$state` but never mutated or reassigned
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/org/[slug]/issues/new/+page.svelte:42

  ⚠ $effect used to derive a single value — use $derived instead
    Replace `$effect(() => { x = expr })` with `const x = $derived(expr)` for better reactivity tracking and fewer re-runs.
    src/routes/org/[slug]/settings/+page.svelte:120

  ⚠ Component has 914 meaningful lines (limit: 300). Consider breaking it into smaller components.
    Large components are harder to maintain and test. Extract logical sections into child components or shared utilities.
    src/routes/org/[slug]/settings/+page.svelte:1

  ⚠ Component has 305 meaningful lines (limit: 300). Consider breaking it into smaller components.
    Large components are harder to maintain and test. Extract logical sections into child components or shared utilities.
    src/routes/org/[slug]/settings/templates/new/+page.svelte:1

  ⚠ `name` is wrapped in `$state` but never mutated or reassigned
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/org/[slug]/settings/templates/new/+page.svelte:33

  ⚠ `identifier` is wrapped in `$state` but never mutated or reassigned
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/sign-in/+page.svelte:16

  ⚠ `password` is wrapped in `$state` but never mutated or reassigned (2)
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/sign-in/+page.svelte:17
    src/routes/sign-up/+page.svelte:18

  ⚠ `username` is wrapped in `$state` but never mutated or reassigned
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/sign-up/+page.svelte:16

  ⚠ `email` is wrapped in `$state` but never mutated or reassigned
    If a value never changes, use a plain `let` or `const` instead of `$state()`. Wrapping non-reactive values in `$state` adds overhead for nothing
    src/routes/sign-up/+page.svelte:17

  ⚠ Load function is missing a type annotation or `satisfies` clause (7)
    Add a type annotation like `export const load: PageLoad = ...` or use `satisfies PageLoad` for full type inference.
    src/routes/+layout.server.ts:3
    src/routes/+page.server.ts:3
    src/routes/invite/[id]/+page.server.ts:3
    src/routes/org/[slug]/+layout.server.ts:3
    src/routes/org/+page.server.ts:3
    src/routes/sign-in/+page.server.ts:4
    src/routes/sign-up/+page.server.ts:7

  ⚠ Unused export: Root (17)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts
    src/lib/components/ui/button/index.ts
    src/lib/components/ui/card/index.ts
    src/lib/components/ui/checkbox/index.ts
    src/lib/components/ui/command/index.ts
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/input-group/index.ts
    src/lib/components/ui/input/index.ts
    src/lib/components/ui/kbd/index.ts
    src/lib/components/ui/label/index.ts
    src/lib/components/ui/scroll-area/index.ts
    src/lib/components/ui/separator/index.ts
    src/lib/components/ui/sheet/index.ts
    src/lib/components/ui/sidebar/index.ts
    src/lib/components/ui/skeleton/index.ts
    src/lib/components/ui/textarea/index.ts
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: buttonVariants
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/button/index.ts

  ⚠ Unused export: internal
    Remove the export or add it to a public API surface if it is intentional
    src/convex/_generated/api.js

  ⚠ Unused export: Content (5)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts
    src/lib/components/ui/sidebar/index.ts
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: Footer (4)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Group (5)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts
    src/lib/components/ui/command/index.ts
    src/lib/components/ui/dropdown-menu/index.ts
    src/lib/components/ui/kbd/index.ts
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: GroupAction
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: GroupContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: GroupLabel
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Header (4)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Input (3)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts
    src/lib/components/ui/input-group/index.ts
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Inset
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Menu
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuAction
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuBadge
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuButton
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuSkeleton
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuSub
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuSubButton
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: MenuSubItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Provider (2)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: Rail
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Separator (3)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts
    src/lib/components/ui/dropdown-menu/index.ts
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Sidebar
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarFooter
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarGroupAction
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarGroupContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarGroupLabel
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarHeader
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarInput
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarInset
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenu
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuAction
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuBadge
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuButton
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuSkeleton
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuSub
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuSubButton
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarMenuSubItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarProvider
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarRail
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: SidebarSeparator
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sidebar/index.ts

  ⚠ Unused export: Trigger (4)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts
    src/lib/components/ui/sidebar/index.ts
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: WORKSPACE_CONTEXT_KEY
    Remove the export or add it to a public API surface if it is intentional
    src/lib/workspace-context.ts

  ⚠ Unused export: ISSUE_STATUSES
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/issues/issue-status.ts

  ⚠ Unused export: Description (3)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: Title (3)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: Action
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: Card
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: CardContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: CardDescription
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: CardFooter
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: CardHeader
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: CardTitle
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: CardAction
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/card/index.ts

  ⚠ Unused export: Image
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts

  ⚠ Unused export: Fallback
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts

  ⚠ Unused export: Badge
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts

  ⚠ Unused export: GroupCount
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts

  ⚠ Unused export: AvatarBadge
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts

  ⚠ Unused export: AvatarGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts

  ⚠ Unused export: AvatarGroupCount
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/avatar/index.ts

  ⚠ Unused export: internalQuery
    Remove the export or add it to a public API surface if it is intentional
    src/convex/_generated/server.js

  ⚠ Unused export: action
    Remove the export or add it to a public API surface if it is intentional
    src/convex/_generated/server.js

  ⚠ Unused export: internalAction
    Remove the export or add it to a public API surface if it is intentional
    src/convex/_generated/server.js

  ⚠ Unused export: httpAction
    Remove the export or add it to a public API surface if it is intentional
    src/convex/_generated/server.js

  ⚠ Unused export: createAuthOptions
    Remove the export or add it to a public API surface if it is intentional
    src/convex/betterAuth/auth.ts

  ⚠ Unused export: Portal (4)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/dropdown-menu/index.ts
    src/lib/components/ui/sheet/index.ts
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: Overlay (2)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: Close (2)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: Dialog (2)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogTitle
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogPortal
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogFooter
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogHeader
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogTrigger
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogOverlay
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogDescription
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: DialogClose
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dialog/index.ts

  ⚠ Unused export: CheckboxGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: CheckboxItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuCheckboxGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuCheckboxItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuPortal
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuRadioGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuRadioItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuShortcut
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuSub
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuSubContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuSubTrigger
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: DropdownMenuGroupHeading
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: GroupHeading
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: Label
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: RadioGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: RadioItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: Shortcut (2)
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: Sub
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: SubContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: SubTrigger
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/dropdown-menu/index.ts

  ⚠ Unused export: ScrollAreaScrollbar
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/scroll-area/index.ts

  ⚠ Unused export: Empty
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: Item
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: LinkItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: List
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: Loading
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: Command
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandDialog
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandEmpty
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandLinkItem
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandInput
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandList
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandSeparator
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandShortcut
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: CommandLoading
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/command/index.ts

  ⚠ Unused export: tables
    Remove the export or add it to a public API surface if it is intentional
    src/convex/betterAuth/schema.ts

  ⚠ Unused export: Tooltip
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: TooltipContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: TooltipTrigger
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: TooltipProvider
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: TooltipPortal
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/tooltip/index.ts

  ⚠ Unused export: Sheet
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetClose
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetTrigger
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetPortal
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetOverlay
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetContent
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetHeader
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetFooter
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetTitle
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: SheetDescription
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/sheet/index.ts

  ⚠ Unused export: Addon
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: Button
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: Text
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: Textarea
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: InputGroup
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: InputGroupAddon
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: InputGroupButton
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: InputGroupInput
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: InputGroupText
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused export: InputGroupTextarea
    Remove the export or add it to a public API surface if it is intentional
    src/lib/components/ui/input-group/index.ts

  ⚠ Unused type: Props
    Remove the type export or re-export it from an index file if consumers need it
    src/lib/components/ui/button/index.ts

  ⚠ Unused type: ButtonProps
    Remove the type export or re-export it from an index file if consumers need it
    src/lib/components/ui/button/index.ts

  ⚠ Unused type: ButtonSize
    Remove the type export or re-export it from an index file if consumers need it
    src/lib/components/ui/button/index.ts

  ⚠ Unused type: ButtonVariant
    Remove the type export or re-export it from an index file if consumers need it
    src/lib/components/ui/button/index.ts

  ⚠ Unused type: SidebarStateProps
    Remove the type export or re-export it from an index file if consumers need it
    src/lib/components/ui/sidebar/context.svelte.ts


  ┌─────────────────────────────────────────────────┐
  │  Svelte Doctor  (svelte-doctor)           │
  │                                                 │
  │  Score: 7 / 100  Critical                       │
  │                                                 │
  │  ███░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░       │
  │                                                 │
  │  ✗ 3 errors  ⚠ 303 warnings  77/493 files  25ms │
  └─────────────────────────────────────────────────┘

  Category breakdown:
    Dead Code: 201 warnings  penalty 100.5
    Performance: 66 warnings  penalty 66.0
    Architecture: 17 warnings  penalty 13.6
    Correctness: 3 errors  penalty 13.5
    State & Reactivity: 10 warnings  penalty 12.0

  Run svelte-doctor fix to auto-fix issues with an AI agent.
